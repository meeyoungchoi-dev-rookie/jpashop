spring:
# spring boot는 별도의 설정이 없으면 메모리 모드로 디비를 돌린다
#  datasource:
#    url: jdbc:h2:mem:test;
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_ssql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace